@model IEnumerable<Sistema_Veterinario.DAL.DesparasitacionVacuna>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Producto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mascota)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Producto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mascota.Genero)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DesparasitacionVacunaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DesparasitacionVacunaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DesparasitacionVacunaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
