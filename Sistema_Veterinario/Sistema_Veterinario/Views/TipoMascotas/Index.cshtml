@model IEnumerable<Sistema_Veterinario.DAL.TipoMascota>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionTipo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionTipo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoMascotaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoMascotaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoMascotaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
